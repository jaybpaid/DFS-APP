# PR1 INFRASTRUCTURE HARDENING - DFS Optimizer Environment Variables
# COMMERCIAL GRADE SECURITY CONFIGURATION

# DFS API Key for Authentication (change this in production!)
DFS_API_KEY=dfs-production-key-2024

# Rate Limiting Configuration
SLOWAPI_LIMIT=100/minute
SLOWAPI_BURST=200

# Database Configuration (hardened)
POSTGRES_USER=dfs_user
POSTGRES_PASSWORD=dfs_password_hardened_2024
POSTGRES_DB=dfs_optimizer

# Redis Configuration
REDIS_URL=redis://redis-hardened:6379

# API Server Configuration
NODE_ENV=production
PORT=8080
