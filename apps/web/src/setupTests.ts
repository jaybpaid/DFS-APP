import '@testing-library/jest-dom';

// Custom test utilities can be added here
declare global {
  namespace jest {
    interface Matchers<R> {
      toBeInTheDocument(): R;
      toHaveClass(className: string): R;
    }
  }
}
